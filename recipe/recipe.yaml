context:
  version: "0.37.0"

package:
  name: meilisearch-python
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/m/meilisearch/meilisearch-${{ version }}.tar.gz
    sha256: a8c0e947d9029b26e833d91ba818cdd56be478756521ce8fe6bcc3178e916ed7

build:
  script: ${{ PYTHON }} -m pip install .
  noarch: python
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools
    - wheel
    - pip
  run:
    - python >=${{ python_min }}
    - requests
    - camel-converter
    - pydantic

tests:
  - python:
      imports:
        - meilisearch
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: The python client for Meilisearch API.
  homepage: https://github.com/meilisearch/meilisearch-python
  description: "Python wrapper for the Meilisearch API"
  license: MIT
  license_file: LICENSE
  documentation: https://meilisearch.github.io/meilisearch-python/

extra:
  recipe-maintainers:
    - moritzwilksch
